export type UserType = {
    id: Number
    name:string
    email:string
}

export type SubCategoryType = {
    title: String
    CatId: Number
    id : Number
}

export type CategoriesType = {
    id: Number
    title: String
}

export type CartItemsType = {
    cartid: number,
    title: string,
    price : number
}

